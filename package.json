{
  "name": "crawler-be-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ crawler-be-subgraph",
    "create-local": "graph create --node http://localhost:8020/ crawler-be-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ crawler-be-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 crawler-be-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.3",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
